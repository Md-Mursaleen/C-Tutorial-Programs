#include <stdio.h>
#define mul(a, b) (a) * (b)
int main()
{

    // printf("\nMultiply two numbers....%d",mul(-3*4*2,5+2-1));
    // #undef mul(a,b)
    printf("\nMultiply two numbers....%d", mul(-3 * 4 * 2, 5 + 2 - 1));
    return 0;
}
